# Migration `20201003184249-crossing_dev`

This migration has been generated by Dennis Dang at 10/3/2020, 6:42:49 PM.
You can check out the [state of the schema](schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Wallet" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"deletedAt" timestamp(3)   ,"id" SERIAL,"updatedAt" timestamp(3)  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "Wallet_userId" ON "public"."Wallet"("userId")

ALTER TABLE "public"."Wallet" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201003150033-crossing_dev..20201003184249-crossing_dev
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url      = "postgresql://postgres:postgres@localhost:5432/crossing_dev"
 }
 generator db {
     provider = "go run github.com/prisma/prisma-client-go"
@@ -17,8 +17,9 @@
     discordUsername String?   @unique
     firstName       String?
     lastName        String?
     avatar          Avatar?
+    Wallet          Wallet?
 }
 model Avatar {
     id             Int              @id @default(autoincrement())
@@ -30,8 +31,17 @@
     AvatarsOnRaids AvatarsOnRaids[]
     Action         Action[]
 }
+model Wallet {
+    id        Int       @id @default(autoincrement())
+    createdAt DateTime  @default(now())
+    deletedAt DateTime?
+    updatedAt DateTime  @updatedAt
+    user      User      @relation(fields: [userId], references: [id])
+    userId    Int
+}
+
 model Raid {
     id                 Int                 @id @default(autoincrement())
     createdAt          DateTime            @default(now())
     deletedAt          DateTime?
```


