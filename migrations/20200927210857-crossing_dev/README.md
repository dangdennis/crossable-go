# Migration `20200927210857-crossing_dev`

This migration has been generated by Dennis Dang at 9/27/2020, 9:08:57 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Raid" ADD COLUMN "storyId" integer  NOT NULL ;

ALTER TABLE "public"."Story" DROP CONSTRAINT IF EXiSTS "Story_storyId_fkey",
DROP COLUMN "storyId";

ALTER TABLE "public"."Raid" ADD FOREIGN KEY ("storyId")REFERENCES "public"."Story"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200927205915-crossing_dev..20200927210857-crossing_dev
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url      = "postgresql://postgres:postgres@localhost:5432/crossing_dev"
 }
 generator db {
     provider = "go run github.com/prisma/prisma-client-go"
@@ -41,8 +41,10 @@
     completionProgress Float               @default(0)
     active             Boolean             @default(false)
     playerLimit        Int                 @default(10)
     playerCount        Int                 @default(0)
+    story              Story               @relation(fields: [storyId], references: [id])
+    storyId            Int
     AvatarsOnRaids     AvatarsOnRaids[]
     RaidBossesOnRaids  RaidBossesOnRaids[]
 }
@@ -89,12 +91,10 @@
     createdAt DateTime  @default(now())
     deletedAt DateTime?
     updatedAt DateTime  @updatedAt
     active    Boolean   @default(true)
-    story     Story     @relation("StoryToStory", fields: [storyId], references: [id])
-    storyId   Int
     Event     Event[]
-    Story     Story[]   @relation("StoryToStory")
+    Raid      Raid[]
 }
 // An Event represents a moment in a story in which Avatars can fulfill Actions
 model Event {
```


